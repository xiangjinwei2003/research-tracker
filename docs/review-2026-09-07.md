# 代码审核报告书

## 改动记录

2026年9月7日，回答「/review /loop 审核所有代码，写报告书，之后进行修改，改完之后再审核，再修改直到全绿。」
新建本报告：记录第一轮 6 人独立审查的合并结论，作为后续修改与复审的依据。

---

## 审查范围

模式：本地工作区，外加用户要求的全部应用源码（`src/`）。

工作区 diff：`.gitignore`，`README.md`，`package.json`，`package-lock.json`，`src/lib/store.ts`，`src/lib/store.persist.test.ts`。统计：5 个已跟踪文件，511 行新增，7 行删除；1 个未跟踪文件。

未审：`node_modules/`，`dist/`。

## 审查团

6 席并行，互不看稿。

| 席位 | 角色 |
| --- | --- |
| General | 正确性与可维护性 |
| General-2 | 正确性与可维护性 |
| General-3 | 正确性与可维护性 |
| General-4 | 正确性与可维护性（无计划文档，未开 Plan 席） |
| Tests | 测试覆盖与质量 |
| Security | 可利用的安全问题 |

合并后：9 个 bug，15 个 suggestion，0 个 nit。完整条目在 scratch 审查稿：`/var/folders/0p/l5hdz0254yv7yg28cg36_mtr0000gn/T/grok-xiangjinwei/grok-review-e8069c41.md`。

## 工作区改动本身

`PERSIST_NAME`（`research-tracker-v1`）、README 键名、tsx 写入测试，这三处结论一致：常量与 README 对齐是对的。Tests 指出测试只覆盖写入半程，没有 hydrate / migrate。General 指出新测试文件落在 `src/` 里，`tsc -b`（因此 `npm run build`）会因 `node:*` 类型失败。

## Bug（合并后 9 条）

1. `src/lib/store.persist.test.ts:5`：Node 测试进入 app tsconfig，生产构建失败。
2. `src/lib/store.ts:159`：`JSON.parse` 失败返回 `null`，hydrate 成空 store，下一次 `setItem` 覆盖原 blob。General-2 标 suggestion，其余标 bug。按破坏已有数据记为 bug。
3. `src/lib/store.ts:141`：`setItem` 失败后仍 `pending.clear()`，`pagehide` 无法重试，界面仍显示已保存。
4. `src/components/Board.tsx:171`：`FocusTimer` 只挂在总览且要求有未归档项目。切到日历 / 回顾 / 归档，或归档最后一个项目后，倒计时、完成、提示音、取消都停。
5. `src/components/Header.tsx:78`：メモ 的 `localStorage` 无 try/catch，隐私模式会让整个壳卸载。
6. `src/components/Header.tsx:118`：「清空全部数据」走 `replaceState`，留下 `activeTimer`；仅看 `projects.length`，只剩专注记录时无法清空。
7. `src/components/ProjectCard.tsx:268`：卡片快捷添加与创建对话框标题在 IME 组字时把 Enter 当成提交。`QuickAddTodo` 已有 `isComposing` 判断。
8. `src/lib/store.ts:848`：`respreadAutoColors` 用 `.includes` 判断旧自动配色。预设与用户自选是同一组 oklch，下次 `SCHEMA_VERSION` 升级会按当前数组下标重涂。General 标 suggestion（当前版本不会跑 migrate），General-3 / General-4 标 bug。按下次升级会改用户选色记为 bug。
9. `src/lib/focus.ts:160`：最长连续天数用 `new Date('yyyy-MM-dd')`（UTC 零点）。西半球时区会把 longest 算成 1。General / General-2 标 suggestion（界面是 zh-CN，UTC+8 可用），General-3 标 bug。按辅助函数对非 UTC+8 算错记为 bug。

## Suggestion（合并后 15 条）

覆盖：persist 测试不是真正 round trip；同版本 hydrate 不跑 `normalizeState`；Safari 拖拽缺少 `text/plain`；`store.ts` / `ProjectDialog.tsx` 已超过 1000 行；导入与 migrate 两条愈合路径会分叉；注释写了改动史；多标签 last write wins；导入的 `color` 可写成 CSS `url()` 触发网络请求；GitHub Pages 项目站同源可读 localStorage；import / undo / 计时 / 纯函数几乎无测试；README 键名断言过弱。

Security：生产依赖 `npm audit --omit=dev` 无漏洞；无 `dangerouslySetInnerHTML`；JSON 导入按字段白名单拷贝。无 critical / high。

## 分歧

1. 损坏 JSON 当空库：多数标 bug，General-2 标 suggestion。
2. 配色重铺：多数标 bug，General 标 suggestion（要等下次版本号）。
3. 最长连续天数：General-3 标 bug，另外两人标 suggestion。
4. 多标签覆盖：General-3 标 suggestion，Security 标 nit。

## 后续

按本报告修改：先修 9 个 bug，再补 persist / normalize / 计时测试与安全相关的 color 白名单、README 同源说明。改完后用同一套 6 席复审，直到合并结果 0 bug、0 未处理 suggestion。

## 第一轮修改（2026年9月7日）

已落地：

1. Node 测试排除出 `tsconfig.app.json`，`npm test` 改为 `src/**/*.test.ts`。
2. persist 抽出 `src/lib/persist.ts`：损坏 JSON 抛错并封锁写入；`setItem` 失败保留 pending；失败时 toast。
3. 归一化抽出 `src/lib/normalize.ts`：每次 hydrate 的 `merge` 跑 `normalizeState`；`respreadAutoColors` 按旧自动槽位匹配，且只在 persist 版本小于 6 时跑；`color` 拒绝 `url()`。
4. `FocusTimer` 挂到 Header，总览 / 日历 / 回顾 / 归档共用一个倒计时。
5. メモ 读写加 try/catch。
6. 「清空全部数据」走 `clearAll`（含 sessions 与 `activeTimer`，可撤销）。
7. 卡片快捷添加与创建对话框标题用 `isSubmitEnter`。
8. `streakDays` 用 `date.ts` 的 `parse` 算最长连续天数。
9. 卡片拖拽同时写 `text/plain`。
10. README 写明 GitHub Pages 同源。
11. `store.ts` 从 1014 行收到 773 行。ProjectDialog 仍约 1205 行，本轮只改 IME，未拆文件。

## 第二轮复审与修改（2026年9月7日）

第二轮 6 席仍报 2 个 bug：损坏键时导出/清空走空内存；`storage` 重放后防抖 pending 会把旧快照写回磁盘。已改：

1. `unreadable` 时导出原始 blob，清空仍走 `clearAll`。
2. `getItem` 成功则丢掉该键 pending。
3. envelope 必须带 `state.projects` 数组。
4. `importJSON` 接受 persist envelope。
5. 本周重点 drop 回退 `text/plain`。
6. `sanitizeColor` 去掉注释和转义后再拒绝 `url()`。
7. migrate/merge 共用 `hydratePersisted`；外来 hydrate 清空 undoStack。
8. `completeTimer` 按 `startedAt` 去重；完成副作用只在可见页签跑。
9. 运行中倒计时去掉 `max-sm:w-full`。

## 第三轮复审与修改（2026年9月7日）

第三轮 4 名 general 一致指出：完成副作用加了 `visibilityState !== 'visible'` 判断后，后台到期不记 session、不响铃。这与「页签不在前台也要提醒」相反。已改回到期即 `completeTimer`；跨标签去重仍靠 `startedAt`。Toaster 移到 Header 之前，避免 unreadable toast 发在 Sonner 订阅前。`isSubmitEnter` 排除 `keyCode` 229。`clearAll` 的撤销会恢复 `activeTimer`。

## 第五轮与第六轮（2026年9月7日）

第五轮：导入撤销会误杀之后新开的倒计时；损坏键导出曾被「内存非空」条件挡住。已把撤销拆成 `replace-state` 与 `clear-all`；损坏时导出始终写磁盘原字符串。

第六轮：4 名 general 与 security 无条目。Tests 指出西半球连续天数在 JST 下仍可能假绿。已加 `TZ=America/Los_Angeles` 子进程断言。合并结果 0 bug。
